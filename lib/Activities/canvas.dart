import 'package:flutter/material.dart';

class CanvasActivity extends StatelessWidget {
  const CanvasActivity({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
